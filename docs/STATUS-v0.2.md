# v0.2 status

Branch: `feat/persistence-v0.2`

Current gate:

```text
Room persistence implementation
  -> unit test + assemble debug
  -> manual restart check on Android
  -> merge
```

JNI, GGUF, llama.cpp, vector DB, and model import remain blocked until this gate is complete.
